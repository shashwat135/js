// console.log("2" > 1)
// console.log("02" > 1)

console.log(null > 0)
console.log(null == 0)
console.log(null >= 0)

// reason for null>=0 >> true and other two are false as equality check and comparison check(> < >= <=) are treated different
// comparisons convert null to a number treating it as 0 

console.log("2" === 2)